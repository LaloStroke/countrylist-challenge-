import React from "react";
import CountryCard from "../CountryCard";

const CountryList: React.FC = (): JSX.Element => {
  return (
    <div>
      CountryList
      <CountryCard />
    </div>
  );
};

export default CountryList;
